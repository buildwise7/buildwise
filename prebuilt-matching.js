const normalise = value => String(value || '').toLowerCase();
const tier = {Entry:1,Everyday:1,Mid:2,High:3};
const budgetOrder = ['Under £600','£600–£800','£800–£1,000','£1,000–£1,500','£1,500–£2,000','£2,000–£3,000','£3,000+'];
export function validPrebuilt(item) { return !!item && typeof item.id==='string' && typeof item.name==='string' && typeof item.brand==='string' && typeof item.cpu==='string' && typeof item.gpu==='string' && typeof item.ram==='string' && typeof item.storage==='string' && Number.isFinite(item.price) && /^https:\/\//.test(item.productUrl||'') && Array.isArray(item.useCases) && typeof item.budgetTier==='string'; }
export function rankPrebuilts(catalogue, preferences) { const requested=budgetOrder.indexOf(preferences.budget); const wanted=preferences.performance==='Higher performance'?3:preferences.performance==='Better value'?1:2; return catalogue.filter(validPrebuilt).map(item=>{let score=0, productBudget=budgetOrder.indexOf(item.budgetTier), productTier=tier[item.performanceTier]||1; if(productBudget===requested)score+=46; else score+=Math.max(0,25-Math.abs(productBudget-requested)*10); if(item.useCases.some(use=>normalise(use)===normalise(preferences.use)))score+=42; if(preferences.use==='Mixed use')score+=15; score+=Math.max(0,18-Math.abs(productTier-wanted)*8); return {item,score};}).sort((a,b)=>b.score-a.score||a.item.price-b.item.price).slice(0,4); }
