const heroVisual = document.querySelector('.hero-sculpture');
if (heroVisual) {
  heroVisual.insertAdjacentHTML('afterbegin', '<img class="hero-pc-render" src="./public/images/hero-buildwise-white-green-cutout.png" alt="White desktop PC with green illuminated cooling" />');
}
